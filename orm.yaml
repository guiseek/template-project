default:
  type: mysql
  host: localhost
  port: 3306
  username: root
  password: root
  database: guiseek_dev
  entities:
    - ./libs/**/*.entity.ts
  migrationsTableName: orm_migrations
  migrations:
    - ./seeds.ts
